#include<stdio.h>
void main()
{
 int count=0,i;
 int count char=0;
 char str[20];
 printf("Enter string ");
 gets(str);
 for(i=0; str[i]!=NULL; i++)
 {
   count_char++;
   if(str[i]==' ')
      count_words++;
 }
 printf("Number of characters in string  %d",countchar);
 printf("Number of words in string % d",count+1); 
 getch();
 return 0;
}
